(* Aurochs *)

(** The main module for all your parsing needs! *)

type program (** Aurochs programs are represented using this abstract type.
                 Actually, the program is malloc()'d and represented using C structures. *)
